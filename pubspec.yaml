name: clean_pathern_flutter
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
    # Service locator
  get_it: ^2.0.1
  # Bloc for state management
  flutter_bloc: ^0.21.0
  # Value equality
  equatable: ^2.0.3
  # Functional programming thingies
  dartz: ^0.10.0
  # Remote API
  data_connection_checker: ^0.3.4
  http: ^0.12.0+2
  # Local caches
  shared_preferences: ^0.5.3+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.14


  flutter_lints: ^1.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/jsonToRead.json
  fonts:
    - family: Raleway
      fonts:
        - asset: assets/fonts_el/Raleway-Regular.ttf